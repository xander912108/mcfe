import LeaderConnections from './pages/LeaderConnections';

export default function LeaderConsoleConnections({ darkMode = true }: { darkMode?: boolean }) {
  return <LeaderConnections darkMode={darkMode} />;
}
